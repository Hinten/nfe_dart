# Local development overrides — not published to pub.dev.
# Allows working with local path dependencies while pubspec.yaml
# declares hosted version constraints for pub.dev publication.
dependency_overrides:
  xml_schema_validator:
    path: ../xml_schema_validator
